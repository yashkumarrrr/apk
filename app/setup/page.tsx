// /setup — standalone setup page (outside dashboard layout, no DB required)
import SetupWizard from '@/components/onboarding/SetupWizard'
export default function SetupPage() { return <SetupWizard /> }
